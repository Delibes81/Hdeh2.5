[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # Tell Netlify to ignore Supabase keys in the build output (they are public by design)
  SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_SUPABASE_URL,NEXT_PUBLIC_SUPABASE_ANON_KEY"
